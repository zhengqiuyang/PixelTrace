# F1 重叠区域验证记录

## 验证场景

上传两张不同尺寸的图片：
- 图片 A: 800×600
- 图片 B: 600×400

## 对齐结果

使用 `alignImages(dataA, dataB, { mode: 'center' })` 后：
- 输出尺寸: 800×600（以较大图为基准）
- 图片 A: 无偏移，完整显示
- 图片 B: 居中放置，偏移 (100, 100)，四周填充黑色

## 重叠区域计算

重叠区域 = 两张图片的交集部分：
- 偏移 X: max(0, offsetAx - offsetBx) = max(0, 0 - 100) = 0 → 实际起始 X = 100
- 偏移 Y: max(0, offsetAy - offsetBy) = max(0, 0 - 100) = 0 → 实际起始 Y = 100
- 重叠宽度: min(800, 600) - 100 = 500
- 重叠高度: min(600, 400) - 100 = 300

## diff 计算行为

当前实现（`useImageDiff` → `computeDiff`）：
1. 将两张图绘制到相同尺寸的 canvas 上（`Math.min(wA, wB), Math.min(hA, hB)`）
2. 对齐后两张图尺寸相同（800×600），所以 diff 在全图范围计算
3. 重叠区域外的黑色填充区域在 diff 中表现为"相同"（黑=黑，无差异）

## 结论

- **重叠区域**: diff 正确计算，差异像素被正确标记
- **重叠区域外**: 由于居中填充为黑色，若原图边缘有内容，在重叠区域外会显示为"差异"（原图内容 vs 黑色填充）
- **这是预期行为**: 对齐后的 diff 包含填充区域的差异，用户可通过变更列表查看所有差异区域

## 验证命令

```bash
npm run verify-dpr
```

在浏览器中打开 `scripts/verify-dpr.html`，查看六视图 DPR 断言结果。

## 手动验证路径

1. 上传两张不同尺寸的 PNG（如 800×600 和 600×400）
2. 弹出"图片尺寸不同"对话框
3. 点击"自动对齐（居中）"
4. 切换到高亮视图，查看差异区域
5. 确认重叠区域内的差异被正确标记
6. 运行 `npm run verify-dpr`，确认六视图 DPR 断言全部通过
