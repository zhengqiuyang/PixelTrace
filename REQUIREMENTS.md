# PixelTrace — 详细需求规格说明书 (Requirements Specification)

> **本文档基于 PRODUCT.md 生成，开发前请先阅读 PRODUCT.md 了解产品全貌。**
>
> **版本**: v1.0
> **最后更新**: 2026-09-10
> **来源**: PRODUCT.md v1.0 Draft
> **用法**: 每个需求项 (REQ-xxx) 是一个原子任务，对应一个可独立提交的 PR。验收标准可直接作为 PR 的验收检查清单。

---

## 目录

0. [阅读指南（优先级与依赖说明）](#0-阅读指南)
1. [模块 A: 基础设施与代码修复](#模块-a-基础设施与代码修复)
2. [模块 B: 图片上传与校验](#模块-b-图片上传与校验)
3. [模块 C: 差异计算引擎（含 WebWorker）](#模块-c-差异计算引擎)
4. [模块 D: 缩放与平移（核心缺失）](#模块-d-缩放与平移)
5. [模块 E: 六种比较视图](#模块-e-六种比较视图)
6. [模块 F: 手动/自动对齐（核心缺失）](#模块-f-手动自动对齐)
7. [模块 G: 变更列表系统](#模块-g-变更列表系统)
8. [模块 H: 工具栏、状态栏与设置面板](#模块-h-工具栏状态栏与设置面板)
9. [模块 I: 键盘快捷键系统](#模块-i-键盘快捷键系统)
10. [模块 J: 导出与剪贴板](#模块-j-导出与剪贴板)
11. [模块 K: 错误处理与加载状态](#模块-k-错误处理与加载状态)
12. [模块 L: 响应式与无障碍](#模块-l-响应式与无障碍)
13. [附录 1: 依赖关系总图](#附录-1-依赖关系总图)
14. [附录 2: 建议迭代里程碑](#附录-2-建议迭代里程碑)

---

## 0. 阅读指南

### 0.1 优先级定义

| 级别 | 含义 | 说明 |
|------|------|------|
| **P0** | 核心功能 | 必须实现。缺失则产品核心场景（上传 → 对比 → 定位差异）不成立。对应 PRODUCT.md 中 🔴 核心缺失项 + 附录 A 中 🔴 高优先级问题 |
| **P1** | 重要增强 | 显著提升可用性。对应 PRODUCT.md v1.1 路线图 + 附录 A 中 🟡 中优先级问题 |
| **P2** | 锦上添花 | 增值功能。对应 v1.2 路线图 + 附录 A 中 🟢 低优先级问题 |

### 0.2 依赖标注规则

每个需求项标注 `依赖: REQ-xxx, REQ-yyy`。被依赖项未合入前，本项不应启动开发（或以 stub 方式并行开发，但合入顺序必须保证）。

### 0.3 通用验收标准（适用于所有需求项，不再逐项重复）

- [ ] 遵循 PRODUCT.md §15 视觉规范（配色、字体、发光特效）
- [ ] 遵循 PRODUCT.md §16 无障碍规范（aria-label、焦点环、对比度）
- [ ] 新增组件/hooks 放置在 PRODUCT.md §14.2 规定的目录位置
- [ ] `npm run build` 无错误无警告
- [ ] 在 Chrome 与 Safari 最新版下手动验证通过

---

## 模块 A: 基础设施与代码修复

> 对应 PRODUCT.md 附录 A 代码问题清单中的结构性问题。**所有其他模块的前置条件。**

### REQ-A1 【P0】修复 ComparisonView imports 位置

- **来源**: 附录 A 问题 1
- **功能描述**: `ComparisonView.jsx` 的 import 语句写在文件底部，可能导致变量提升问题（依赖 hoisting）且违反代码规范。
- **任务**: 将所有 import 移至文件顶部；补充 ESLint 规则 `import/first` 防止回归。
- **输入/输出**: 无运行时行为变化，纯代码整理。
- **边界条件**: 确认无循环依赖；如存在则提取共享模块到 `utils/`。
- **验收标准**:
  1. 所有 import 位于文件最顶部（注释除外）
  2. ESLint `import/first` 规则启用且通过
  3. 六种视图切换功能回归测试通过
- **依赖**: 无

### REQ-A2 【P0】修复 SliderView overlay canvas 尺寸计算

- **来源**: 附录 A 问题 2
- **功能描述**: SliderView 的差异叠加 overlay canvas 与底图尺寸/缩放不一致，导致叠加层错位或被拉伸。
- **任务**: overlay canvas 的像素尺寸必须等于底图绘制尺寸（`displayWidth × devicePixelRatio`），并通过 CSS 变换与底图完全重合；在窗口 resize 与缩放时同步更新。
- **验收标准**:
  1. 上传 1920×1080 两图，overlay 差异框与底图差异位置逐像素对齐（截图比对误差 ≤ 1px）
  2. 缩放到 50% 和 200% 时 overlay 仍对齐
  3. 浏览器窗口 resize 后仍对齐
- **依赖**: 无（但完整验证依赖 REQ-D1）

### REQ-A3 【P0】建立 constants.js 与全局类型定义

- **功能描述**: 为后续所有模块提供统一常量与数据结构（PRODUCT.md §8.1 的 `DiffRegion`、§6.1.2 缩放级别表、§11.1 快捷键表、§12.1 错误文案表、§15.1 配色 CSS 变量）。
- **任务**:
  - `src/utils/constants.js`: 导出 `ZOOM_LEVELS`、`DEFAULT_SETTINGS`、`ERROR_MESSAGES`、`VIEW_TYPES`
  - `src/index.css`: 写入 §15.1 全部 CSS 变量
  - `src/types.js`: JSDoc 定义 `DiffRegion`、`ImageMeta`、`AppSettings` 类型
- **验收标准**:
  1. 常量命名与 PRODUCT.md 一致，无魔法数字散落
  2. CSS 变量在 DevTools 中可覆盖生效
- **依赖**: 无

### REQ-A4 【P1】基础状态管理重构

- **功能描述**: 当前用零散 useState，无法支撑缩放/平移/设置/变更列表的联动。按 PRODUCT.md §14.3 数据流重构为 `useReducer` 集中管理。
- **任务**: 建立 `src/store/reducer.js`，state 包含 `{ images, view, zoom, pan, settings, diffResult, selectedRegion }`；提供 `useAppContext`。
- **验收标准**:
  1. 视图切换后 zoom/pan 状态保留（不重置）
  2. settings 修改即时生效于所有视图
- **依赖**: REQ-A3

---

## 模块 B: 图片上传与校验

### REQ-B1 【P0】上传校验管线（类型/大小/尺寸）

- **功能描述**: 按 PRODUCT.md §5.1.4 流程实现完整校验：文件类型 → 文件大小(50MB) → 图片尺寸(4096×4096)，每步失败走 §12.1 对应错误路径。
- **输入**: File 对象。**输出**: `{ ok, image, meta }` 或 `{ ok: false, error: { code, message, canContinue, canScale } }`。
- **交互流程**: 类型不支持 → 红框提示 3 秒恢复；大小/尺寸超限 → 弹窗提供「继续上传 / 缩放后继续 / 取消」三选项。
- **边界条件**:
  - GIF 取第一帧；BMP/WebP 支持；SVG、PDF 拒绝
  - 0 字节文件、损坏文件 → 「无法读取该图片，文件可能已损坏」
  - 尺寸为 0 或解码失败的图片按损坏处理
- **验收标准**:
  1. 上传 .svg 被拒绝且提示文案与 §12.1 一致
  2. 上传 60MB PNG 出现三选项弹窗，选「缩放后继续」可成功加载
  3. 上传改名 .png 的文本文件报「损坏」
  4. 两张都上传成功后 300ms 内自动进入比较视图
- **依赖**: REQ-A3

### REQ-B2 【P0】上传区完整状态机（5 态）

- **功能描述**: 实现 PRODUCT.md §4.3 的 5 个状态：空/拖拽悬浮/已上传/加载中/错误，含对应视觉与交互。
- **交互流程**: dragenter → 边框变绿+发光+「释放以上传」；drop/leave 恢复；加载中骨架屏+像素风动画且禁用交互；已上传显示预览+「✕ 移除」；错误态点击可重试。
- **边界条件**: 拖放多个文件时只取第一个；拖放非图片文件视为格式错误；移除已上传图片后若正在比较页需回到上传页。
- **验收标准**:
  1. 5 个状态均可人工触发，视觉与 §4.3 表格一致
  2. 加载中重复点击/拖放无副作用
  3. 移除按钮可正确清空对应槽位（左/右互不影响）
- **依赖**: REQ-A3

### REQ-B3 【P1】粘贴上传

- **功能描述**: 在上传页监听 `paste` 事件，Ctrl+V 粘贴截图图片填充到第一个空槽位。
- **边界条件**: 剪贴板无图片 → Toast「剪贴板中未检测到图片」2 秒；剪贴板有多项时取第一个 image 类型项。
- **验收标准**:
  1. macOS/Windows 截图工具截图后 Ctrl+V 成功填充
  2. 复制纯文本后 Ctrl+V 出现 Toast 提示
- **依赖**: REQ-B1, REQ-B2

### REQ-B4 【P1】图片信息展示

- **功能描述**: 已上传图片下方显示 `文件名 / 宽×高 / 文件大小(KB/MB) / 格式`（§5.1.5）；侧边栏信息面板（§5.2）复用同一 `ImageMeta` 数据。
- **边界条件**: 文件大小 < 1MB 显示 KB，否则 MB（保留 1 位小数）；色彩空间不可读取时省略该字段而非显示 "unknown"。
- **验收标准**:
  1. 上传 1920×1080 PNG 后显示 `1920 × 1080 | 2.4 MB | PNG` 格式
  2. 比较页侧边栏信息面板字段与 §5.2 表格一一对应
- **依赖**: REQ-B1, REQ-A4

---

## 模块 C: 差异计算引擎

### REQ-C1 【P0】核心差异算法（utils/imageDiff.js）

- **功能描述**: 纯函数实现逐像素 RGB 差异计算：`computeDiff(imageDataA, imageDataB, { threshold }) → { diffImageData, diffCount, diffPixels: Uint8Array(0/1 mask) }`。相同像素输出黑色/透明，差异像素输出彩色（供相减视图）与掩码（供高亮视图复用，避免二次计算）。
- **输入/输出**: 见上；`threshold` 为 0–100，映射到每通道容差。
- **边界条件**:
  - 两图尺寸不同时仅在重叠区域计算（配合 REQ-F1 的对齐逻辑）
  - 带透明通道像素按合成到黑底后比较
- **验收标准**:
  1. 构造 10×10 测试图：1 个像素 R 值差 5（threshold=30）判定相同，差 50 判定不同
  2. `diffCount` 与掩码中非零数量一致
  3. 1920×1080 计算耗时 < 500ms（§13.3 基准）
- **依赖**: REQ-A3

### REQ-C2 【P0】区域聚类算法（Connected Component + Merge）

- **功能描述**: 输入差异掩码，输出 `DiffRegion[]`（结构见 §8.1）：4/8 邻域连通域标记 → 按 `minArea` 过滤 → 距离 < `mergeDistance` 的区域合并 → 按从上到下、从左到右编号。
- **边界条件**:
  - 差异像素为 0 → 返回空数组，UI 显示「未检测到差异」
  - 区域数 > 500 时截断并提示（防列表爆炸），统计面板显示真实值
- **验收标准**:
  1. 两个相距 5px 的差异块在 mergeDistance=10 时合并为 1 个区域，mergeDistance=2 时为 2 个
  2. 小于 minArea 的孤立噪点被过滤
  3. 编号顺序符合从上到下从左到右
- **依赖**: REQ-C1

### REQ-C3 【P0】WebWorker 差异计算（workers/diffWorker.js）

- **功能描述**: 将 REQ-C1/C2 迁入 WebWorker，主线程通过消息 `{ type, imageDataA, imageDataB, settings }` 调用，支持进度回报 `{ type: 'progress', percent }`；不支持的降级为主线程计算（§12.1 WebGL/Worker 降级条款）。
- **边界条件**: Worker 消息传输用 Transferable（`ImageData.data.buffer`）避免拷贝； Worker 崩溃或 postMessage 失败时自动降级并 Toast 提示。
- **验收标准**:
  1. 计算期间主线程可响应（拖动滑块不卡顿，Performance 面板确认无 >50ms 长任务）
  2. 4096×4096 计算耗时 < 3s 且进度条正常推进（§13.3 基准）
  3. 关闭 Worker 开关（设置面板）后功能仍完整
- **依赖**: REQ-C1, REQ-C2

### REQ-C4 【P0】useImageDiff Hook

- **功能描述**: 封装 diff 计算生命周期：`useImageDiff(imageA, imageB, settings) → { diffImageData, regions, stats, computing, progress, recompute }`。内部做防抖（阈值滑块拖动时 150ms debounce）、结果缓存（settings 未变的重复调用直接返回缓存）、组件卸载时终止 Worker 并释放 buffer。
- **验收标准**:
  1. 快速拖动阈值滑块不产生计算堆积（同一时刻最多 1 个在途计算，旧结果被丢弃）
  2. 卸载后无 Worker 泄漏（DevTools 确认线程数回落）
  3. 相同参数二次调用无重复计算（加日志验证）
- **依赖**: REQ-C3, REQ-A4

### REQ-C5 【P1】分级渲染与性能优化

- **功能描述**: 落实 §12.3/§13.1：zoom < 50% 用 0.5x 降采样渲染；>2000px 大图首次加载即降采样，放大到 100%+ 切换全分辨率；已渲染结果缓存为 ImageBitmap。
- **验收标准**:
  1. 4096×4096 图首次渲染 < 1s（§13.3）
  2. 缩放/平移过程中 rAF 帧率 ≥ 55fps（Performance 面板验证）
  3. 内存不随反复缩放持续增长（Heap snapshot 对比）
- **依赖**: REQ-C4, REQ-D1

---

## 模块 D: 缩放与平移（核心缺失）

> PRODUCT.md §1.4 标注的 🔴 核心缺失。对应附录 A 问题 3。

### REQ-D1 【P0】useZoomPan Hook 与画布容器

- **功能描述**: 实现 `useZoomPan(containerRef) → { zoom, pan, setZoom, setPan, zoomAtPoint, fitToWindow, reset }`，及共享画布容器组件 `CanvasStage`：所有视图渲染于此容器内，统一受 zoom/pan 控制（通过 CSS `transform: translate() scale()` 实现以保证 60fps）。
- **输入/输出**: zoom 取值 10%~1600%，离散级别表见 §6.1.2；pan 为 `{x, y}` 像素偏移。
- **交互流程**（§6.1/§6.2 全量）:
  - 滚轮/Ctrl+滚轮：以鼠标位置为中心缩放（`zoomAtPoint` 数学：新 pan 使鼠标下图像点保持不动）
  - 空格按住或中键拖动：平移，光标 `grab`→`grabbing`
  - 双击：100% ↔ 适应窗口切换
- **边界条件**:
  - 缩放上限/下限 clamp，不越界
  - 拖出边界有弹性回弹（overshoot ≤ 100px，松手 200ms 弹回）
  - 触控板捏合（wheel + ctrlKey）与滚轮区分处理，避免浏览器页面缩放（preventDefault）
  - 窗口 resize 时若当前为「适应窗口」则重新计算
- **验收标准**:
  1. 滚轮缩放时鼠标指向的像素不移动（误差 ≤ 2px）
  2. 缩放级别严格落在 §6.1.2 级别表（滚轮步进）
  3. 空格+拖动平移流畅，光标状态正确切换
  4. 缩放动画 150ms 平滑过渡、60fps
  5. 底部状态栏缩放百分比实时同步
  6. 缩放中画布中心显示百分比 300ms 后消失（§6.1.4）
- **依赖**: REQ-A4

### REQ-D2 【P0】缩放工具栏 ZoomBar

- **功能描述**: 实现 §9.2 工具栏：缩小/放大按钮、缩放下拉菜单（预设 25%/50%/100%/200%/400%/适应窗口）、适应窗口按钮。步进缩放走级别表逐级。
- **验收标准**:
  1. 每个按钮功能正确且快捷键（`+` `-` `0` `Ctrl+0`）联动
  2. 下拉菜单当前级别高亮
- **依赖**: REQ-D1, REQ-I1（快捷键可后补，先留 TODO）

### REQ-D3 【P1】方向键平移与 Home 回原点

- **功能描述**: 方向键平移 10px、Shift+方向键 100px、Home 回原点 (0,0)、PageUp/PageDown 按画布高度滚动（§6.2.1、§11.1）。
- **验收标准**: 各按键位移量准确；平移实时更新状态栏坐标。
- **依赖**: REQ-D1, REQ-I1

---

## 模块 E: 六种比较视图

> 所有视图共享 REQ-D1 的 CanvasStage（zoom/pan 一致生效），共享 REQ-C1 的 diffImageData。

### REQ-E1 【P0】滑块视图 SliderView（默认视图）

- **功能描述**: §7.1。两图叠加，垂直分割线左原始右修改后，可拖动。
- **交互流程**: 拖动分割线 / 点击画布跳转 / ←→ 步进 5% / Home/End 到两端。
- **边界条件**: 分割线位置 clamp 在 0%~100%；拖动分割线时不触发画布平移（事件冒泡需阻止）；zoom/pan 时分割线跟随图像坐标系（而非视口）。
- **验收标准**:
  1. 分割线视觉符合规范：2px 绿色 #00FF88 发光 + 40px 圆形手柄 ◄► + 左右标签
  2. 拖动流畅 60fps，两图无缝无错位
  3. 点击画布分割线立即跳到点击 x 位置
  4. 缩放 200% 后拖动分割线，两图仍逐像素对齐
  5. 键盘 ←→ 步进 5%，Home/End 到两端
  6. 作为默认视图：进入比较页首个展示
- **依赖**: REQ-D1, REQ-B1（本项需重做含 REQ-A2 修复）

### REQ-E2 【P0】分割视图 SplitView

- **功能描述**: §7.2。左右并排，共享同一 zoom/pan 状态。
- **边界条件**: 两图尺寸不同时右侧留白区域显示网格底纹；点击某侧高亮该侧边框。
- **验收标准**:
  1. 左侧滚轮缩放，右侧同步缩放（同一变换矩阵）
  2. 中间分割线 2px 绿色发光
  3. 鼠标在任一侧移动，另一侧显示对应位置十字同步线（P2 可延后，见 REQ-E7）
- **依赖**: REQ-D1

### REQ-E3 【P0】淡化视图 FadeView

- **功能描述**: §7.3。两图同位叠加，底部滑块控制透明度 0~100%。
- **边界条件**: 滑块拖动与画布平移事件互不干扰；点击画布跳转需排除点击在手柄上。
- **验收标准**:
  1. 滑块 0% 纯原始图、100% 纯修改后图、50% 各半透明
  2. 右上角实时显示 `50%` 标签
  3. ←→ 步进 5%；点击画布区域滑块按点击 x 比例跳转
- **依赖**: REQ-D1

### REQ-E4 【P0】闪烁视图 BlinkView

- **功能描述**: §7.4。定时交替显示两图，频率可调 100~2000ms，默认 500ms；空格暂停/恢复；顶部状态指示器（红点=原始，绿点=修改后）。
- **边界条件**: 切走视图时必须清除 interval/rAF（内存泄漏防护）；暂停时保持当前帧并维持指示器状态；标签页不可见时（`visibilitychange`）自动暂停。
- **验收标准**:
  1. 速度滑块拖到 100ms 能明显高频闪烁，2000ms 低频
  2. 空格暂停/恢复生效，指示器文字同步「当前: 原始/修改后」
  3. 切换到其他视图再切回，定时器无重复（验证只有一个在跑）
  4. 切走视图后 CPU 占用回落
- **依赖**: REQ-D1

### REQ-E5 【P0】相减视图 SubtractView

- **功能描述**: §7.5。渲染 REQ-C1 的 diffImageData：相同区域黑、差异区域按差异强度发亮。
- **边界条件**: 全黑结果（无差异）时显示「两张图片完全相同」提示；悬停像素差值需从 diff 数据读取而非重算。
- **验收标准**:
  1. 视觉符合 §7.5：黑底 + 彩色差异 + 底部说明栏
  2. 悬停显示 `(x, y)` 坐标与 RGB 差值（如 `ΔR: 32 ΔG: 0 ΔB: -15`）
  3. 缩放/平移正常工作且悬停数值对应正确像素
- **依赖**: REQ-C1, REQ-D1

### REQ-E6 【P0】高亮视图 HighlightView

- **功能描述**: §7.6，最复杂视图。灰度底图（原图转灰度、亮度降 40%）+ 红色差异像素（rgba(255,50,50,0.8)）+ REQ-C2 的区域编号虚线边框；底部阈值滑块实时重算。
- **交互流程**: 拖动阈值滑块 → useImageDiff debounce 重算 → 区域边框与变更列表同步更新；点击列表区域定位（见 REQ-G2）。
- **边界条件**: 阈值拖动期间显示轻量 loading（不阻塞）；区域框绘制在图像坐标系，随缩放平移变换但线宽保持 2px 视觉宽（除以 zoom 补偿）。
- **验收标准**:
  1. 视觉符合 §7.6 规范（灰度底图、红色差异、2px 虚线框、#N 编号）
  2. 阈值从 10 拖到 90，差异像素与区域数单调不增（人工验证趋势）
  3. 选中区域边框变实线绿色
  4. 「显示区域框」开关即时生效
- **依赖**: REQ-C4, REQ-D1, REQ-G1

### REQ-E7 【P2】视图高级功能包

- **功能描述**: §7.1–7.6 各「高级功能」合并为一个 PR：滑块视图差异热力叠加与 3×3 网格参考线开关、分割视图十字同步线与差异闪烁、相减视图增益 1x~10x 与彩虹色相映射、高亮视图最小区域面积/合并距离快捷调节。
- **验收标准**: 每个子功能有开关且默认关闭时行为与基础版完全一致（回归无差异）。
- **依赖**: REQ-E1 ~ REQ-E6 全部合入

---

## 模块 F: 手动/自动对齐

> PRODUCT.md §1.4 标注的 🔴 核心缺失；路线图列为 v1.2，但因尺寸不同图片当前无法正确对比，判定为 P0（自动对齐）+ P1（手动微调）。

### REQ-F1 【P0】尺寸不同检测与自动对齐

- **功能描述**: §6.3.1。两图尺寸不同时弹窗「⚠ 检测到图片尺寸不同」，提供 [自动对齐] [保持原样]；自动对齐以较大图为基准缩放小图、居中放置，差异计算仅在重叠区域进行。
- **边界条件**:
  - 宽高比差异过大（> 25%）时弹窗额外警告「对齐后可能失真」
  - 「保持原样」时以左上角对齐，重叠区域外显示为无差异
- **验收标准**:
  1. 1920×1080 vs 1600×900 触发弹窗；相同尺寸不触发
  2. 自动对齐后 diff 掩码尺寸 = 较大图尺寸，无越界崩溃
  3. 两种选择均可进入比较页且状态栏标注当前对齐模式
- **依赖**: REQ-C1

### REQ-F2 【P1】对齐模式（手动微调）

- **功能描述**: §6.3.2。工具栏新增「对齐模式」入口：两图半透明叠加、透明度 0~100% 可调、十字线+网格辅助线、拖动上层图微调偏移、数值输入框精确设置 dx/dy/旋转(±180°，步进 0.1°)/缩放(50%~200%，步进 1%)；确认后以该变换重新计算差异。
- **边界条件**: 对齐模式下禁用视图切换；Esc 取消并回滚到进入前状态；变换参数写入 diff 计算管线（REQ-C1 接受可选 transform）。
- **验收标准**:
  1. 人为偏移 20px 的两张相同图，在对齐模式拖回后差异归零
  2. 精确输入 dx=20 生效且 undo 回滚正确
  3. 对齐结果在六种视图中一致生效
- **依赖**: REQ-F1, REQ-D1, REQ-C4

---

## 模块 G: 变更列表系统

### REQ-G1 【P0】变更列表基础版增强（缩略图 + 统计）

- **功能描述**: §8.2/§8.5。列表项 = 64×64 Base64 缩略图（区域裁剪自修改后图）+ 编号 + `宽×高` + `像素数 (百分比)`；底部差异统计面板（总像素/差异像素/差异占比/区域数/最大最小区域）。缩略图懒生成（§13.2）。
- **边界条件**: 区域 > 500（REQ-C2 截断）时列表顶部提示；缩略图生成放在 rAF 空闲批处理，每帧最多 10 个。
- **验收标准**:
  1. 列表项视觉与 §8.2 ASCII 图一致
  2. 统计面板数值与 diff 结果精确一致（自动化断言）
  3. 500+ 区域时列表滚动流畅（虚拟滚动或分批渲染）
- **依赖**: REQ-C2, REQ-C4

### REQ-G2 【P0】点击定位与悬浮联动

- **功能描述**: §4.4/§8.3。点击区域 → 画布平移+缩放使该区域居中（缩放至区域占视口 50%），边框闪烁 2 次；悬浮列表项 → 画布对应边框变绿色加粗；画布 hover 区域 → 列表项反向高亮（P2）。
- **边界条件**: 连续快速点击多个区域只保留最后一次定位动画；当前视图非高亮视图时点击列表项自动切换到高亮视图再定位。
- **验收标准**:
  1. 点击 #3 区域后该区域居中显示且缩放合理（部分在视口外判定失败）
  2. 边框闪烁恰好 2 次后保持选中实线绿框
  3. 悬浮联动无明显延迟（< 50ms）
- **依赖**: REQ-G1, REQ-D1, REQ-E6

### REQ-G3 【P1】排序与筛选

- **功能描述**: §8.4。排序：编号/面积大→小/面积小→大/位置左→右/位置上→下；筛选：全部/仅大区域/仅小区域（面积阈值取区域面积中位数 × 2）。
- **验收标准**: 各排序结果可用自动化断言验证；筛选切换不触发重新计算（仅 UI 过滤）；区域编号保持不变（排序不改 id）。
- **依赖**: REQ-G1

### REQ-G4 【P1】聚焦模式与右键菜单

- **功能描述**: §8.3。双击列表项进入聚焦模式：画布只显示该区域（裁剪放大），Esc 退出；右键弹菜单：复制坐标 / 复制缩略图 / 导出该区域 PNG。
- **验收标准**:
  1. 双击后视口仅含该区域及 10% 边距，Esc 完全恢复原视口
  2. 复制坐标得到 `(x, y, width, height)` 文本；导出区域 PNG 尺寸与区域一致
- **依赖**: REQ-G2

---

## 模块 H: 工具栏、状态栏与设置面板

### REQ-H1 【P0】主工具栏完善

- **功能描述**: §9.1。左：返回（Esc）+ 图片信息按钮；中：6 视图切换按钮（当前视图高亮）；右：设置（`,`）、导出（Ctrl+E）。底部状态栏（§4.1）：缩放百分比 | 鼠标坐标 (x, y) | 当前像素 RGB | 变更区域数。
- **边界条件**: 鼠标移出画布坐标显示 `--`；RGB 取自原始图（非 diff 图）；按钮均带 aria-label 与快捷键 tooltip。
- **验收标准**:
  1. 六个视图按钮切换正确且快捷键 1~6 生效
  2. 状态栏四段信息实时更新（鼠标移动 < 16ms 延迟）
  3. 返回按钮回到上传页且两张图保留（重置按钮才清空）
- **依赖**: REQ-A4, REQ-D1

### REQ-H2 【P1】设置面板 SettingsModal

- **功能描述**: §9.3。四组设置：差异检测（阈值 0-100 / 最小区域面积 / 合并距离）、显示（差异框/编号/网格/十字线开关、高亮颜色）、缩放平移（滚轮速度 %、平滑动画、百分比提示）、性能（Worker 开关、渲染精度 1x/0.5x/0.25x）。恢复默认 + 确定按钮。
- **边界条件**: 阈值/面积/距离修改需触发 REQ-C4 recompute（debounce）；设置持久化到 localStorage，下次会话恢复；Modal 打开时焦点困在面板内（focus trap），Esc 关闭。
- **验收标准**:
  1. 修改阈值关闭面板后重新打开数值保留；刷新页面后保留（localStorage）
  2. 「恢复默认」立即还原全部控件与实际行为
  3. 关闭 Worker 开关后 diff 走主线程且功能正常
  4. 渲染精度 0.25x 时大图明显更快且视觉可接受
- **依赖**: REQ-A4, REQ-C4, REQ-D1

### REQ-H3 【P1】侧边栏折叠

- **功能描述**: §4.4。侧边栏默认展开，工具栏按钮折叠/展开，折叠动画 200ms；画布区域宽度联动重算「适应窗口」。
- **验收标准**: 折叠后画布利用释放的空间重新 fit；展开恢复；1200px 以下默认折叠。
- **依赖**: REQ-H1, REQ-D1

---

## 模块 I: 键盘快捷键系统

### REQ-I1 【P0】useKeyboard Hook 与全局快捷键

- **功能描述**: §11.1。实现 `src/hooks/useKeyboard.js`：统一 keydown/keyup 监听，按作用域分发（全局 / 滑块视图 / 闪烁视图 / 高亮视图），快捷键映射表放 constants.js。
- **边界条件**:
  - 输入框/modal 聚焦时全局快捷键不触发（除 Esc）
  - `?` 为 Shift+/；浏览器保留键（Ctrl+T 等）不拦截
  - Space 需区分「按住=临时平移」（全局）与「单击=闪烁暂停」（闪烁视图），以 keydown/keyup + 视图判断
  - `Ctrl+C` 只在画布聚焦时劫持为复制视图，否则放行系统复制
- **验收标准**:
  1. §11.1 表中全部全局快捷键逐条人工验证通过
  2. 文本输入时按 `1`~`6` 不切换视图
  3. 快捷键冲突无：闪烁视图中 Space 只暂停不触发平移
- **依赖**: REQ-A4, REQ-D1

### REQ-I2 【P1】快捷键帮助面板 ShortcutHelp

- **功能描述**: §11.2。按 `?` 打开 Modal，按分组（视图切换/缩放平移/操作）展示 `<kbd>` 样式列表，Esc 或 ✕ 关闭。
- **验收标准**: 内容与 §11.1 表完全一致；打开时全局快捷键暂停（除 Esc/?）。
- **依赖**: REQ-I1

### REQ-I3 【P1】高亮视图列表导航键

- **功能描述**: §11.1 作用域快捷键：高亮视图 `1`-`9` 选中区域、`0` 取消、`Tab`/`Shift+Tab` 在变更列表间移动焦点并同步定位。
- **验收标准**: 与全局数字视图切换的冲突处理：高亮视图下数字优先选区域（文档化该行为）；Tab 循环到末尾回到第一个。
- **依赖**: REQ-I1, REQ-G2

---

## 模块 J: 导出与剪贴板

### REQ-J1 【P0】导出当前视图 PNG

- **功能描述**: §10.1。`Ctrl+E` / `Ctrl+S` / 工具栏导出按钮：将当前视图所见（含叠加层、区域框、分割线）合成导出为 PNG 下载，文件名 `pixeltrace_{view}_{timestamp}.png`。
- **边界条件**: 导出使用当前视口内容、当前分辨率 × devicePixelRatio；导出中按钮 loading 防重复点击。
- **验收标准**:
  1. 六种视图分别导出，图片内容与画布所见一致（人工比对）
  2. 下载文件可正常打开且为 PNG
  3. 快速连按 Ctrl+E 只产生一次下载
- **依赖**: REQ-A4（useExport hook）

### REQ-J2 【P1】复制到剪贴板

- **功能描述**: §10.3。`Ctrl+C`（画布聚焦时）将当前视图写入系统剪贴板（`ClipboardItem` + `canvas.toBlob('image/png')`），成功 Toast「已复制到剪贴板」。
- **边界条件**: 非 HTTPS 或不支持 ClipboardItem 时降级 Toast 提示「当前浏览器不支持复制，已改为下载」并走下载。
- **验收标准**: 复制后粘贴到微信/飞书/Word 显示正确图片；Firefox 降级路径可用。
- **依赖**: REQ-J1

### REQ-J3 【P1】全分辨率导出与变更列表 CSV

- **功能描述**: §10.1。`Ctrl+Shift+E` 导出原始分辨率合成 PNG；导出菜单新增「差异报告」(PNG + JSON 元数据) 与「变更列表 CSV」(id,x,y,width,height,pixels,percentage)。
- **验收标准**:
  1. 全分辨率导出图片尺寸 = 图像原始尺寸
  2. CSV 用 Excel 打开无乱码（UTF-8 BOM），行数 = 区域数
- **依赖**: REQ-J1, REQ-G1

### REQ-J4 【P2】导出选项面板

- **功能描述**: §10.2/附录 A。导出按钮点击打开选项 Modal：格式 PNG/JPEG/WebP、质量滑块 92% 默认、含区域标记、含水印开关，确认后执行。
- **验收标准**: 各格式导出文件类型正确；JPEG 质量 50% vs 95% 文件大小差异明显；勾选项生效。
- **依赖**: REQ-J1

---

## 模块 K: 错误处理与加载状态

### REQ-K1 【P0】统一错误 Toast/弹窗系统

- **功能描述**: §12.1 八种错误场景的统一呈现：轻量错误用 Toast（2~3 秒自动消失），需用户决策的用弹窗（尺寸过大/大小过大/尺寸不同）。文案严格使用 §12.1 表格。
- **任务**: 新建 `src/components/Toast.jsx` 与 `Dialog.jsx`，提供 `useToast()`；错误码→文案映射放 constants.js。
- **验收标准**:
  1. §12.1 表 8 行场景逐一触发，文案与处理方案完全一致
  2. 多个 Toast 排队不重叠；弹窗打开时页面不可交互
  3. Toast/弹窗均有 aria-live 通告
- **依赖**: REQ-A3

### REQ-K2 【P0】加载与计算进度状态

- **功能描述**: §12.2。上传中像素风加载动画（`█▀█` 依次高亮）；diff 计算中画布显示进度条 `正在计算差异... 45%`（数据来自 REQ-C3 progress 消息）；首次渲染 200ms 模糊到清晰过渡。
- **验收标准**:
  1. 4096×4096 计算时进度条平滑推进且结束时消失
  2. 计算期间 UI 可交互（可切换视图，切换后计算不中断）
  3. 动画遵循 `prefers-reduced-motion`（减弱时改为静态提示）
- **依赖**: REQ-C3, REQ-K1

### REQ-K3 【P1】内存不足与降级处理

- **功能描述**: §12.1。diff 计算抛出内存类异常时捕获，Toast「图片过大导致内存不足，建议缩小图片」并自动降级为 0.5x 渲染精度重试一次；Worker 不可用时静默降级主线程。
- **验收标准**: 用 4096×4096 图在低内存环境（或模拟 throw）下不白屏，降级后可完成计算。
- **依赖**: REQ-K1, REQ-C3, REQ-C5

---

## 模块 L: 响应式与无障碍

### REQ-L1 【P1】响应式移动端适配

- **功能描述**: §15.4 断点：< 768px 上传区纵向排列、视图按钮堆叠、侧边栏隐藏（可通过按钮浮出）；768~1200px 侧边栏可折叠；触屏支持单指平移、双指捏合缩放。
- **边界条件**: 移动端 hover 联动失效时列表点击定位仍可用；触屏事件与鼠标事件统一用 Pointer Events。
- **验收标准**:
  1. 375px 宽（iPhone SE）下上传页与比较页无横向滚动条、无元素重叠
  2. iOS Safari 捏合缩放作用于画布而非页面
  3. 768/1200 断点切换布局正确
- **依赖**: REQ-D1, REQ-G1

### REQ-L2 【P2】无障碍专项

- **功能描述**: §16 全量落地：Tab 可聚焦所有交互元素、2px 绿色虚线焦点环、全按钮 aria-label、状态变化 aria-live、对比度 WCAG AA (4.5:1) / 强调色 AAA (7:1)、图像区域 aria-description。
- **验收标准**:
  1. 纯键盘（无鼠标）可完成「上传→切换视图→定位区域→导出」全流程
  2. axe DevTools 扫描无 critical 违规
  3. 对比度用工具实测达标
- **依赖**: 全模块基本完成后做专项 pass

---

## 附录 1: 依赖关系总图

```
REQ-A3 (常量/类型) ──┬─→ REQ-A4 (状态管理) ─┬─→ D 缩放平移 (D1→D2/D3)
                     ├─→ B 上传 (B1→B2→B3/B4) │
                     └─→ K1 错误系统          ├─→ E 六视图 (E1..E6)
                                              ├─→ H1 工具栏 → H2 设置 → H3 侧边栏
C 差异引擎 (C1→C2→C3→C4→C5) ─────────────────┤
  C1 ─→ F1 自动对齐 → F2 手动对齐             ├─→ I1 快捷键 → I2/I3
  C4 ─→ G1 变更列表 → G2 定位 → G3/G4         └─→ J1 导出 → J2/J3/J4
E1..E6 全部 ─→ E7 高级功能包
全部基本完成后 ─→ L1 响应式 → L2 无障碍
```

**关键路径**: A3 → A4 → D1 → E1 → G2（「上传 → 缩放定位差异」是产品核心价值链，最先打通）

## 附录 2: 建议迭代里程碑

| 里程碑 | 范围 | 退出标准 |
|--------|------|----------|
| **M1 — 核心可用 (P0 全部)** | A1-A3, B1-B2, C1-C4, D1-D2, E1-E6, F1, G1-G2, H1, I1, J1, K1-K2 | 用户可完整走通三大核心场景（PRODUCT.md §2.2 场景 A/B/C），附录 A 的 🔴 问题全部关闭 |
| **M2 — v1.1 完善 (P1)** | A4, B3-B4, C5, D3, F2, G3-G4, H2-H3, I2-I3, J2-J3, K3, L1 | PRODUCT.md §17 v1.1 清单全部勾选，附录 A 🟡 问题关闭 |
| **M3 — v1.2 增值 (P2)** | E7, J4, L2 及后续 v1.2 路线图项 | 附录 A 🟢 问题关闭，v1.2 路线图按需排期 |

---

*本文档共 45 个需求项（P0: 22 项 / P1: 17 项 / P2: 6 项）。执行顺序建议严格遵循附录 1 关键路径；每项合入后勾选对应验收标准并存档于 PR 描述中。*
