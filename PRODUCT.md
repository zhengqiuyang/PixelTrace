# PixelTrace — 产品需求文档 (PRD)

> **版本**: v1.0 Draft  
> **最后更新**: 2026-09-10  
> **产品名称**: PixelTrace (像素追踪)  
> **Slogan**: 逐像素图像差异追踪器  

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户画像与使用场景](#2-用户画像与使用场景)
3. [信息架构](#3-信息架构)
4. [页面与组件规范](#4-页面与组件规范)
5. [核心功能模块](#5-核心功能模块)
6. [图像操作交互规范](#6-图像操作交互规范)
7. [六种比较视图详细规范](#7-六种比较视图详细规范)
8. [变更列表系统](#8-变更列表系统)
9. [工具栏与设置面板](#9-工具栏与设置面板)
10. [导出与分享系统](#10-导出与分享系统)
11. [键盘快捷键系统](#11-键盘快捷键系统)
12. [错误处理与边界情况](#12-错误处理与边界情况)
13. [性能优化策略](#13-性能优化策略)
14. [技术架构](#14-技术架构)
15. [视觉设计规范](#15-视觉设计规范)
16. [无障碍设计](#16-无障碍设计)
17. [未来迭代路线图](#17-未来迭代路线图)

---

## 1. 产品概述

### 1.1 产品定位

PixelTrace 是一款**纯前端、浏览器端运行**的逐像素图像差异分析工具。用户上传两张图片后，工具通过像素级比对，以多种可视化视图展示两张图之间的所有差异，帮助设计师、开发者、测试人员快速定位 UI 变更、设计回归、截图对比等问题。

### 1.2 核心价值主张

| 价值点 | 说明 |
|--------|------|
| **纯本地处理** | 所有计算在浏览器内完成，图片不上传任何服务器，100% 隐私安全 |
| **零门槛使用** | 无需注册、无需安装、打开即用 |
| **像素级精度** | 不是"看起来像"，而是逐像素 RGB 差值计算，不遗漏任何差异 |
| **6 种可视化视图** | 滑块、分割、淡化、闪烁、相减、高亮，覆盖所有对比场景 |
| **差异区域聚类** | 自动将零散差异像素聚合为有意义的"变更区域"，生成可操作列表 |
| **大图支持** | 像素尺寸不设上限（仅限制单张 50MB），WebWorker 后台计算不阻塞 UI |
| **像素风科技感 UI** | Press Start 2P 字体 + 扫描线纹理 + 霓虹绿配色，极致极客体验 |

### 1.3 竞品对比

| 能力 | DiffChecker | PixelTrace (本产品) |
|------|-------------|---------------------|
| 纯前端离线运行 | ❌ | ✅ |
| 图片缩放/平移 | ✅ | ✅ (待实现) |
| 图片对齐 | ✅ | ✅ (待实现) |
| 变更区域聚类 | ✅ | ✅ |
| 高亮视图 | ✅ | ✅ |
| 相减视图 | ✅ | ✅ |
| 闪烁视图 | ✅ | ✅ |
| 键盘快捷键 | 部分 | ✅ 全覆盖 |
| 暗色主题 | 仅亮色 | ✅ 像素风暗色 |
| 开源 | ❌ | ✅ |

### 1.4 当前已实现 vs 待实现

> 更新于 2026-09-11。这张表长期停留在「规划期」状态（把已完成的缩放/平移、
> 拖动对齐、快捷键、错误处理都标成未实现），现已按代码实际情况重写。

| 模块 | 状态 | 待完善 |
|------|------|--------|
| 图片上传（拖放 + 点击） | ✅ 5 态状态机、超限弹窗决策 | — |
| 图片预览 / 移除 / 信息 | ✅ | — |
| 6 种比较视图 | ✅ | 局部聚焦模式（双击列表项） |
| 缩放 / 平移 | ✅ 滚轮、拖拽、方向键、双击、预设级别 | 旋转 90° |
| 拖动对齐 | ✅ 居中缩放对齐 + 重叠区域计算 | 手动微调偏移 |
| 变更列表 | ✅ 缩略图、点击定位、悬停高亮、排序、筛选、统计 | 右键菜单、批量选择 |
| 工具栏与设置面板 | ✅ 四组设置，改动即时生效 | 导出选项面板（格式 / 质量 / 水印） |
| 导出与分享 | ✅ PNG 导出、复制到剪贴板 | PNG 全分辨率、差异报告、变更列表 CSV |
| 快捷键 | ✅ 含帮助面板 | — |
| 错误处理 | ✅ | — |
| 响应式布局 | ✅ 基础版 | 移动端触控适配 |
| 性能 | ✅ WebWorker + 进度 | 渲染精度 (1x / 0.5 / 0.25) |

---

## 2. 用户画像与使用场景

### 2.1 用户画像

| 角色 | 场景 | 痛点 |
|------|------|------|
| **UI 设计师** | 对比设计稿 v1 vs v2，检查像素级偏差 | 截图工具无法精确标出差异 |
| **前端开发者** | 对比实现效果 vs 设计稿，找出布局偏移 | 需要肉眼逐像素对比 |
| **QA 测试** | 对比 UI 回归测试截图 | 手动对比耗时且易遗漏 |
| **摄影师** | 对比后期处理前后效果 | 需要专业软件才能叠加对比 |
| **文档维护** | 对比文档截图更新前后 | 大量截图逐一核对 |

### 2.2 核心使用场景

**场景 A: 前端开发者检查 UI 实现**
1. 打开 PixelTrace
2. 左侧上传设计稿截图
3. 右侧上传浏览器实现截图
4. 使用"高亮"视图查看所有差异区域
5. 在变更列表中逐项确认每个偏差
6. 修复后重新截图对比

**场景 B: 设计师对比设计迭代**
1. 上传初版设计
2. 上传修改版设计
3. 使用"滑块"视图左右拖动对比
4. 使用"淡化"视图渐变过渡，感受整体变化
5. 导出差异图作为评审材料

**场景 C: QA 回归测试**
1. 上传基线截图
2. 上传当前版本截图
3. 使用"相减"视图快速判断是否有差异
4. 使用"闪烁"视图让差异"跳动"出来
5. 查看变更列表确认所有变更点

---

## 3. 信息架构

```
PixelTrace
├── 首页 (上传页)
│   ├── Logo + 标语
│   ├── 图片上传区 (左: 原始, 右: 修改后)
│   │   ├── 拖放上传区
│   │   ├── 点击选择按钮
│   │   └── 已上传图片预览
│   └── 底部提示文字
│
├── 比较结果页
│   ├── 顶部工具栏
│   │   ├── 左侧: 返回按钮、图片信息
│   │   ├── 中间: 视图切换器 (6种视图)
│   │   └── 右侧: 设置、导出、重置
│   ├── 主比较区域
│   │   ├── 画布容器 (支持缩放/平移)
│   │   └── 视图控件 (滑块线/淡化滑条等)
│   ├── 侧边栏 (可折叠)
│   │   ├── 变更列表
│   │   │   ├── 变更区域 #1 [缩略图] [坐标] [面积]
│   │   │   ├── 变更区域 #2 ...
│   │   │   └── 变更区域 #N ...
│   │   ├── 图片信息面板
│   │   │   ├── 原始: 尺寸 / 文件大小 / 格式
│   │   │   └── 修改后: 尺寸 / 文件大小 / 格式
│   │   └── 差异统计
│   │       ├── 总像素数
│   │       ├── 差异像素数
│   │       ├── 差异百分比
│   │       └── 变更区域数
│   └── 底部状态栏
│       ├── 当前缩放比例
│       ├── 鼠标坐标 (x, y)
│       └── 当前像素 RGB 值
│
└── 设置面板 (Modal)
    ├── 差异检测设置
    │   ├── 阈值滑块 (0-100)
    │   ├── 最小区域面积
    │   └── 合并距离
    ├── 显示设置
    │   ├── 显示/隐藏差异框
    │   ├── 显示/隐藏网格
    │   └── 差异高亮颜色
    └── 性能设置
        ├── WebWorker 开关
        └── 渲染精度 (1x/0.5x/0.25x)
```

---

## 4. 页面与组件规范

### 4.1 全局布局

```
┌──────────────────────────────────────────────────────────┐
│  [Logo]  PIXEL TRACE          [设置] [导出] [重置]        │  ← 固定顶栏 h=56px
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │                                                 │    │
│  │              主比较画布区域                       │    │  ← flex: 1
│  │         (缩放/平移/视图渲染)                     │    │
│  │                                                 │    │
│  │                                                 │    │
│  └─────────────────────────────────────────────────┘    │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  100% │ (1280, 720) │ #FF3366 │ 变更: 12 个区域         │  ← 固定底栏 h=32px
└──────────────────────────────────────────────────────────┘
```

### 4.2 上传页布局

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                      █ PIXEL TRACE                       │
│                  逐像素图像差异追踪器                      │
│                                                          │
│  ┌────────────────────┐      ┌────────────────────┐     │
│  │                    │      │                    │     │
│  │   [+]              │  VS  │   [+]              │     │
│  │                    │      │                    │     │
│  │   原始图片          │      │   修改后图片        │     │
│  │   拖放图片或点击选择 │      │   拖放图片或点击选择 │     │
│  │   JPG / PNG / WebP │      │   JPG / PNG / WebP │     │
│  │                    │      │                    │     │
│  └────────────────────┘      └────────────────────┘     │
│                                                          │
│              上传两张图片以开始比较差异                      │
│                                                          │
│  ▪▪▪ PIXELTRACE v1.0 — 像素级图像差异分析 ▪▪▪            │
└──────────────────────────────────────────────────────────┘
```

### 4.3 上传组件状态

| 状态 | 视觉表现 | 交互 |
|------|----------|------|
| **空状态** | 虚线边框 + [+] 图标 + 提示文字 | 点击/拖放 |
| **拖拽悬浮** | 边框变绿 + 发光 + 文字变为"释放以上传" | 保持拖拽 |
| **已上传** | 显示图片预览 + "✕ 移除"按钮 | 点击移除重新上传 |
| **加载中** | 骨架屏 + 像素风加载动画 | 禁用交互 |
| **错误** | 红色边框 + 错误图标 + 错误信息 | 点击重试 |

### 4.4 侧边栏

侧边栏默认展开，可通过工具栏按钮折叠/展开。

```
┌─────────────┐
│ 变更区域 (12)│  ← 标题 + 总数
├─────────────┤
│ ┌─┐ #1      │
│ │1│ 320×240 │  ← 缩略图 + 编号 + 尺寸
│ └─┘ 1,200px │     + 像素数
├─────────────┤
│ ┌─┐ #2      │
│ │2│ 160×80  │
│ └─┘ 480px   │
├─────────────┤
│ ...         │
├─────────────┤
│ 差异统计     │
│ 总像素: 921K │
│ 差异: 2.3%   │
│ 区域: 12 个  │
└─────────────┘
```

**侧边栏交互规则:**
- 点击某个变更区域 → 画布自动平移+缩放，将该区域居中显示，并短暂高亮闪烁
- 悬浮某个变更区域 → 画布上对应区域边框加粗变色
- 变更区域列表支持按面积/位置/编号排序
- 支持筛选: 显示全部 / 仅大区域 / 仅小区域

---

## 5. 核心功能模块

### 5.1 图片上传模块

#### 5.1.1 上传方式

| 方式 | 说明 |
|------|------|
| **拖放上传** | 将图片文件从文件管理器/浏览器拖放到上传区域 |
| **点击选择** | 点击上传区域，弹出系统文件选择对话框 |
| **粘贴上传** | 按 Ctrl+V 粘贴剪贴板中的图片 (来自截图) |
| **URL 加载** | 输入图片 URL (v2.0) |

#### 5.1.2 支持格式

| 格式 | 支持 | 说明 |
|------|------|------|
| JPG/JPEG | ✅ | 最常用格式 |
| PNG | ✅ | 支持透明通道 |
| WebP | ✅ | 现代高效格式 |
| GIF | ✅ | 取第一帧 |
| BMP | ✅ | 位图格式 |
| HEIC | ⚠️ | 部分浏览器支持 |
| SVG | ❌ | 矢量图不适用像素对比 |
| PDF | ❌ | v2.0 支持 |

#### 5.1.3 图片约束

| 参数 | 限制 | 超出处理 |
|------|------|----------|
| 尺寸 / 像素数 | **不限制**（4096 × 4096 闸门已取消） | —— |
| 最大文件大小 | 50 MB | 弹窗提示，建议压缩 |

> 尺寸不设上限的代价：超出浏览器 canvas 上限（Chrome 约 16384px / 2.68 亿像素）的图片
> 无法参与差异计算，此时提示"图片过大导致内存不足，建议缩小图片"。

#### 5.1.4 上传流程

```
用户拖放/选择文件
       │
       ▼
  验证文件类型 ──── 不支持 ──→ 显示错误提示
       │
       ▼ (通过)
  验证文件大小 ──── 过大 ────→ 弹窗「继续上传 / 取消」
       │
       ▼ (通过)
  读取为 Image 对象（不校验尺寸）
       │
       ▼
  存储到状态
       │
       ▼
  两张都上传完毕 ──→ 自动进入比较视图
```

#### 5.1.5 图片预览区信息

已上传图片下方显示:
```
image_name.png
1920 × 1080 | 2.4 MB | PNG
```

### 5.2 图片信息面板

位于侧边栏底部，展示两张图片的元数据:

| 字段 | 说明 |
|------|------|
| 文件名 | 原始文件名 |
| 尺寸 | 宽 × 高 (px) |
| 文件大小 | KB / MB |
| 格式 | PNG / JPG / WebP 等 |
| 色彩空间 | sRGB / Display P3 (如果可读取) |
| 差异像素数 | 不同的像素数量 |
| 差异百分比 | 差异像素占总像素的比例 |
| 变更区域数 | 聚类后的区域数量 |

---

## 6. 图像操作交互规范

### 6.1 缩放 (Zoom)

这是**最核心的缺失功能**，必须实现。

#### 6.1.1 缩放方式

| 方式 | 操作 | 说明 |
|------|------|------|
| **滚轮缩放** | 鼠标滚轮 (Ctrl+滚轮 也可) | 以鼠标位置为中心缩放 |
| **快捷键缩放** | `+` / `-` / `0` | 放大/缩小/重置为 100% |
| **按钮缩放** | 工具栏 +/- 按钮 | 线性步进缩放 |
| **双击缩放** | 双击画布 | 切换 100% ↔ 适应窗口 |
| **捏合缩放** | 触控板双指捏合 | 移动端/触控板支持 |

#### 6.1.2 缩放级别

```
25% → 33% → 50% → 67% → 75% → 80% → 90% → 100% → 125% → 150% → 200% → 300% → 400% → 800%
```

预设快捷级别: 25%, 50%, 100%, 200%, 400%, 适应窗口

#### 6.1.3 缩放行为

- **以鼠标位置为中心**: 缩放时，鼠标指向的像素点保持不动
- **缩放范围**: 10% ~ 1600%
- **缩放速度**: 每次滚轮步进 10% (可配置)
- **平滑缩放**: 缩放过程有 150ms 过渡动画
- **适应窗口**: 自动计算缩放比使图片完整显示在画布区域

#### 6.1.4 缩放时的视觉反馈

- 缩放过程中画布中心短暂显示当前缩放百分比 (300ms 后消失)
- 工具栏实时显示缩放百分比

### 6.2 平移 (Pan)

#### 6.2.1 平移方式

| 方式 | 操作 | 说明 |
|------|------|------|
| **鼠标拖动** | 按住空格 + 拖动 / 中键拖动 | 标准平移操作 |
| **方向键** | ↑↓←→ | 按像素微调平移 |
| **Page Up/Down** | PageUp / PageDown | 按画布高度滚动 |
| **触控拖动** | 单指拖动 (移动端) | 触屏操作 |

#### 6.2.2 平移行为

- 平移时鼠标光标变为 `grabbing` (拖动中) / `grab` (可拖动)
- 平移边界: 图片可以被拖出可视区域，但有弹性回弹效果
- 双击空白区域: 自动平移回原点 (0, 0)
- 平移时实时更新状态栏的坐标信息

### 6.3 对齐 (Alignment)

#### 6.3.1 自动对齐

当两张图片尺寸不同时，系统自动提示:
```
┌──────────────────────────────────────┐
│ ⚠ 检测到图片尺寸不同                    │
│ 原始: 1920×1080  修改后: 1600×900      │
│                                      │
│ [自动对齐]  [保持原样]                  │
└──────────────────────────────────────┘
```

点击"自动对齐"后:
1. 将两张图缩放到相同尺寸 (以较大图为基准)
2. 将较小的图居中放置
3. 差异计算只在重叠区域进行

#### 6.3.2 手动对齐

提供"对齐模式"，允许用户:
- 拖动其中一张图微调位置 (x, y 偏移)
- 输入精确偏移数值
- 旋转角度微调 (±180°，步进 0.1°)
- 缩放微调 (50%~200%，步进 1%)

对齐模式下:
- 两张图以半透明叠加显示
- 透明度可调 (0%~100%)
- 辅助十字线 + 网格参考线

### 6.4 鼠标光标状态

| 状态 | 光标 | 触发条件 |
|------|------|----------|
| 默认 | `default` | 未操作时 |
| 可拖动 | `grab` | 鼠标在画布上且可平移时 |
| 拖动中 | `grabbing` | 按住鼠标平移时 |
| 缩放中 | `zoom-in` / `zoom-out` | Ctrl+滚轮时 |
| 滑块操作 | `ew-resize` | 滑块视图拖动分割线时 |

---

## 7. 六种比较视图详细规范

### 7.1 滑块视图 (Slider)

**默认视图**

#### 功能描述
左右两张图并排叠加，中间有一条可拖动的垂直分割线。拖动分割线，左侧显示原始图，右侧显示修改后图。

#### 交互规范

| 操作 | 效果 |
|------|------|
| 鼠标拖动分割线 | 左右平滑切换 |
| 点击画布任意位置 | 分割线跳转到点击位置 |
| 键盘 ← → | 分割线步进移动 (每次 5%) |
| Home / End | 分割线跳到最左/最右 |

#### 视觉规范

```
┌──────────────┬──────────────────┐
│   原始图片    │    修改后图片     │
│              │                  │
│              │                  │
│         ─────┼───── ← 分割线   │
│              │   (绿色发光)     │
│              │                  │
│              │                  │
└──────────────┴──────────────────┘
         ▲                    ▲
      [原始]              [修改后]
      标签                 标签
```

- 分割线: 2px 宽，绿色 (#00FF88)，带发光效果
- 分割线手柄: 圆形，40px，带 `◄ ►` 图标
- 标签: 9px Press Start 2P 字体，半透明黑底

#### 高级功能

- **差异叠加**: 在分割线两侧叠加差异热力图 (可选开关)
- **对齐参考线**: 显示 3×3 网格参考线 (可选开关)

### 7.2 分割视图 (Split)

#### 功能描述
两张图并排显示，缩放和平移完全同步。适合同时查看两张图的同一区域。

#### 交互规范

| 操作 | 效果 |
|------|------|
| 滚轮缩放 | 两张图同步缩放 |
| 拖动平移 | 两张图同步平移 |
| 点击左侧/右侧 | 高亮该侧 |

#### 视觉规范

```
┌────────────────┬─┬────────────────┐
│                │█│                │
│   原始图片      │█│   修改后图片    │
│                │█│                │
│                │█│                │
└────────────────┴─┴────────────────┘
```

- 中间分割线: 2px 绿色，带发光
- 两张图共享同一个缩放/平移状态

#### 高级功能

- **差异闪烁**: 在两张图的差异区域交替闪烁高亮
- **十字同步线**: 鼠标在任一侧移动时，另一侧显示对应位置的十字线

### 7.3 淡化视图 (Fade)

#### 功能描述
两张图叠加在同一位置，通过底部滑块控制透明度，实现渐变过渡效果。

#### 交互规范

| 操作 | 效果 |
|------|------|
| 拖动底部滑块 | 控制透明度 0%~100% |
| 点击画布 | 滑块跳转到点击位置 |
| 键盘 ← → | 透明度步进调整 (每次 5%) |

#### 视觉规范

```
┌────────────────────────────────────┐
│                                    │
│          叠加显示的图片              │
│                                    │
│              [原始]                │
│                                    │
├────────────────────────────────────┤  ← 底部滑块轨道
│ ○──────────────○                  │  ← 滑块手柄
└────────────────────────────────────┘
```

- 滑块轨道: 底部 10% 区域，半透明背景
- 滑块手柄: 16px 圆形，绿色发光
- 百分比显示: 右上角 `50%` 标签

### 7.4 闪烁视图 (Blink)

#### 功能描述
在原位快速交替显示两张图，任何移动/变化的内容都会产生"跳动"效果。

#### 交互规范

| 操作 | 效果 |
|------|------|
| 拖动速度滑块 | 调整闪烁频率 (100ms~2000ms) |
| 点击暂停按钮 | 暂停/恢复闪烁 |
| 空格键 | 暂停/恢复闪烁 |

#### 视觉规范

```
┌────────────────────────────────────┐
│          [● 当前: 原始]             │  ← 顶部状态指示器
│                                    │
│          交替闪烁的图片              │
│                                    │
├────────────────────────────────────┤
│ 闪烁速度 [====○========] 500ms     │  ← 底部控制栏
└────────────────────────────────────┘
```

- 状态指示器: 圆点 + 文字，显示当前显示的是哪张图
- 圆点颜色: 原始=红色，修改后=绿色
- 默认闪烁速度: 500ms

### 7.5 相减视图 (Subtract)

#### 功能描述
将两张图逐像素相减，相同区域为黑色，不同区域显示为彩色 (差异强度越高越亮)。

#### 交互规范

| 操作 | 效果 |
|------|------|
| 鼠标悬停 | 显示该像素的 RGB 差值 |
| 滚轮 | 缩放 |
| 拖动 | 平移 |

#### 视觉规范

```
┌────────────────────────────────────┐
│                                    │
│          黑色背景 + 彩色差异         │
│          (差异区域发亮)             │
│                                    │
├────────────────────────────────────┤
│ 相减视图 — 黑色=相同，彩色=差异       │
└────────────────────────────────────┘
```

- 底部信息栏: 说明颜色含义
- 鼠标悬停时显示像素坐标和 RGB 差值

#### 高级功能

- **增益调节**: 可调节差异放大倍数 (1x~10x)，让微弱差异更明显
- **色相映射**: 将差异映射为彩虹色谱，不同颜色代表不同差异方向

### 7.6 高亮视图 (Highlight)

**最复杂的视图，包含变更列表系统**

#### 功能描述
将差异像素直接标注在原图上 (灰度底图 + 红色差异像素)，自动将差异像素聚类为"变更区域"，每个区域有编号、边框和缩略图。

#### 交互规范

| 操作 | 效果 |
|------|------|
| 点击变更列表中的区域 | 画布平移+缩放到该区域 |
| 悬浮变更列表中的区域 | 画布上对应区域边框高亮 |
| 拖动阈值滑块 | 实时重新计算差异 |
| 切换显示/隐藏区域框 | 控制是否显示编号边框 |

#### 视觉规范

```
┌────────────────────────────────────┬─────────────┐
│                                    │ 变更区域 (12)│
│  灰度底图 + 红色差异像素             │ ┌─┐ #1      │
│  + 编号边框                         │ │1│ 320×240  │
│                                    │ └─┘ 1,200px │
│  ┌──┐                              ├─────────────┤
│  │#1│    ┌──┐                      │ ┌─┐ #2      │
│  └──┘    │#2│                      │ │2│ 160×80   │
│          └──┘                      │ └─┘ 480px   │
├────────────────────────────────────┤ ...         │
│ 阈值 [========○====] 30            ├─────────────┤
│ ☑ 显示区域框                        │ 差异统计     │
└────────────────────────────────────┴─────────────┘
```

- 灰度底图: 原图转灰度，亮度降低 40%
- 差异像素: 红色 (255, 50, 50)，不透明度 80%
- 区域边框: 2px 虚线红色，选中时变为实线绿色
- 编号标签: 区域左上角 `#1`, `#2` ...

#### 高级功能

- **阈值调节**: 5~100，实时重新计算
- **最小区域面积**: 可配置，过滤噪点
- **合并距离**: 相距多近的差异像素合并为同一区域
- **多次扫描**: 用不同参数多次扫描，结果合并

---

## 8. 变更列表系统

### 8.1 变更区域数据结构

```typescript
interface DiffRegion {
  id: number;           // 区域编号 (从 1 开始)
  x: number;            // 左上角 x 坐标
  y: number;            // 左上角 y 坐标
  width: number;        // 区域宽度
  height: number;       // 区域高度
  pixels: number;       // 差异像素数量
  center: { x: number; y: number };  // 中心点
  percentage: number;   // 占总差异的百分比
  thumbnail: string;    // Base64 缩略图 (64×64)
}
```

### 8.2 列表项展示

每个变更区域在列表中显示:
```
┌──────────────────────────┐
│ ┌────┐  #1               │
│ │缩略│  320 × 240        │
│ │图  │  1,200 px (12.3%) │
│ └────┘                   │
└──────────────────────────┘
```

### 8.3 列表交互

| 操作 | 效果 |
|------|------|
| **点击区域** | 画布自动平移+缩放，将该区域居中，边框闪烁 2 次 |
| **双击区域** | 进入"聚焦模式"，只显示该区域，按 Esc 退出 |
| **悬浮区域** | 画布上对应区域边框高亮为绿色 |
| **右键区域** | 弹出菜单: 复制坐标 / 复制缩略图 / 导出区域 |
| **拖动排序** | 手动调整区域顺序 (v2.0) |
| **批量选择** | Ctrl+点击多选，批量导出 (v2.0) |

### 8.4 排序与筛选

| 排序方式 | 说明 |
|----------|------|
| 按编号 | 默认，从上到下、从左到右 |
| 按面积 (大→小) | 最大差异优先 |
| 按面积 (小→大) | 最小差异优先 |
| 按位置 (左→右) | 按 x 坐标排序 |
| 按位置 (上→下) | 按 y 坐标排序 |

| 筛选方式 | 说明 |
|----------|------|
| 显示全部 | 默认 |
| 仅大区域 | 面积 > 阈值 |
| 仅小区域 | 面积 < 阈值 |
| 按颜色筛选 | 只显示某种颜色范围的差异 (v2.0) |

### 8.5 差异统计面板

```
┌─────────────────────────┐
│ 差异统计                 │
├─────────────────────────┤
│ 总像素数:    2,073,600  │
│ 差异像素:      47,820   │
│ 差异占比:       2.3%    │
│ 变更区域:         12    │
│ 最大区域:   320×240     │
│ 最小区域:    16×8       │
└─────────────────────────┘
```

---

## 9. 工具栏与设置面板

### 9.1 主工具栏

```
┌──────────────────────────────────────────────────────────┐
│ [← 返回]  [图片信息]    ⊞ ⫿ ◐ ⚡ ⊖ ◎    [≡ 设置] [↓ 导出] │
└──────────────────────────────────────────────────────────┘
```

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| ← 返回 | 返回上传页 | Esc |
| 图片信息 | 展开/折叠图片信息面板 | I |
| ⊞ 滑块 | 切换到滑块视图 | 1 |
| ⫿ 分割 | 切换到分割视图 | 2 |
| ◐ 淡化 | 切换到淡化视图 | 3 |
| ⚡ 闪烁 | 切换到闪烁视图 | 4 |
| ⊖ 相减 | 切换到相减视图 | 5 |
| ◎ 高亮 | 切换到高亮视图 | 6 |
| ≡ 设置 | 打开设置面板 | , |
| ↓ 导出 | 导出当前视图为 PNG | Ctrl+E |

### 9.2 缩放工具栏

```
┌──────────────────────────────────────────────────┐
│ [🔍-] [100% ▼] [🔍+] [⊡ 适应] [↻ 旋转] [⌨ 快捷键] │
└──────────────────────────────────────────────────┘
```

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| 🔍- | 缩小 | - |
| 100% ▼ | 缩放下拉菜单 (预设级别) | 0 |
| 🔍+ | 放大 | + |
| ⊡ 适应 | 适应窗口 | Ctrl+0 |
| ↻ 旋转 | 顺时针旋转 90° | R |
| ⌨ 快捷键 | 显示快捷键列表 | ? |

### 9.3 设置面板

以 Modal 形式打开:

```
┌────────────────────────────────────────────────────┐
│ 设置                                        [✕]    │
├────────────────────────────────────────────────────┤
│                                                    │
│ ▸ 差异检测                                          │
│   阈值          [========○====] 30                  │
│   最小区域面积   [====○========] 50 px              │
│   合并距离       [======○======] 10 px              │
│                                                    │
│ ▸ 显示设置                                          │
│   ☑ 显示差异区域边框                                 │
│   ☑ 显示区域编号                                     │
│   ☐ 显示像素网格                                     │
│   ☐ 显示十字参考线                                   │
│   差异高亮颜色  [🔴] (可选颜色)                       │
│                                                    │
│ ▸ 缩放与平移                                        │
│   滚轮缩放速度   [=====○=======] 10 %               │
│   ☑ 平滑缩放动画                                     │
│   ☑ 缩放时显示百分比                                 │
│                                                    │
│ ▸ 性能                                              │
│   ☑ 使用 WebWorker (推荐)                           │
│   渲染精度     (○) 1x  ( ) 0.5x  ( ) 0.25x        │
│                                                    │
│                    [恢复默认]  [确定]                 │
└────────────────────────────────────────────────────┘
```

> 实现说明（2026-09-11）：面板已落地，「性能」组目前只有 WebWorker 开关。
> 「渲染精度 (1x/0.5/0.25)」**暂未实现** —— 它需要六个视图连同差异计算一起
> 改成可变分辨率渲染，是独立的一块工作，没有放进面板当摆设控件。
> 「确定」按钮改为「完成」（所有改动即时生效，不存在待提交的草稿态）。

---

## 10. 导出与分享系统

### 10.1 导出格式

| 格式 | 说明 | 快捷键 |
|------|------|--------|
| PNG | 当前视图截图 | Ctrl+E |
| PNG 全分辨率 | 原始分辨率导出 | Ctrl+Shift+E |
| 差异报告 | PNG + 元数据 JSON | - |
| 变更列表 CSV | 所有变更区域的坐标和面积 | - |

### 10.2 导出选项

```
┌────────────────────────────────────┐
│ 导出设置                           │
├────────────────────────────────────┤
│ 格式: (●) PNG  ( ) JPEG  ( ) WebP │
│ 质量: [========○====] 92%          │
│ ☑ 包含变更区域标记                   │
│ ☐ 包含水印                         │
│                                    │
│           [取消]  [导出]            │
└────────────────────────────────────┘
```

### 10.3 复制到剪贴板

- `Ctrl+C`: 复制当前视图到剪贴板
- 粘贴到飞书/钉钉/微信等即时通讯工具直接分享

---

## 11. 键盘快捷键系统

### 11.1 快捷键列表

| 快捷键 | 功能 | 作用域 |
|--------|------|--------|
| `1` ~ `6` | 切换视图 | 全局 |
| `+` / `=` | 放大 | 全局 |
| `-` | 缩小 | 全局 |
| `0` | 重置缩放为 100% | 全局 |
| `Ctrl+0` | 适应窗口 | 全局 |
| `Ctrl+1` | 缩放到 100% | 全局 |
| `Ctrl+2` | 缩放到 200% | 全局 |
| `↑↓←→` | 平移画布 (10px) | 全局 |
| `Shift+方向键` | 平移画布 (100px) | 全局 |
| `Home` | 回到原点 | 全局 |
| `F` | 全屏切换 | 全局 |
| `Space` (按住) | 临时平移模式 | 全局 |
| `Ctrl+C` | 复制到剪贴板 | 全局 |
| `Ctrl+S` | 导出 PNG | 全局 |
| `Ctrl+Z` | 撤销缩放/平移 | 全局 |
| `Ctrl+Shift+Z` | 重做 | 全局 |
| `R` | 旋转图片 90° | 全局 |
| `I` | 切换图片信息面板 | 全局 |
| `,` | 打开设置 | 全局 |
| `?` | 显示快捷键帮助 | 全局 |
| `Esc` | 退出/返回 | 全局 |
| `← →` | 滑块视图: 移动分割线 | 滑块视图 |
| `Space` | 闪烁视图: 暂停/恢复 | 闪烁视图 |
| `1-9` | 高亮视图: 选中区域 #1~#9 | 高亮视图 |
| `0` | 高亮视图: 取消选中 | 高亮视图 |
| `Tab` | 变更列表: 下一个区域 | 高亮视图 |
| `Shift+Tab` | 变更列表: 上一个区域 | 高亮视图 |

### 11.2 快捷键帮助面板

按 `?` 打开:

```
┌────────────────────────────────────────┐
│ 键盘快捷键                        [✕]  │
├────────────────────────────────────────┤
│ 视图切换                              │
│   1 ~ 6          切换比较视图          │
│                                        │
│ 缩放与平移                            │
│   + / -          放大 / 缩小          │
│   0              重置为 100%           │
│   Ctrl+0         适应窗口             │
│   ↑↓←→           平移                 │
│   Space (按住)   临时平移             │
│                                        │
│ 操作                                  │
│   Ctrl+C         复制到剪贴板         │
│   Ctrl+S         导出 PNG             │
│   R              旋转 90°             │
│   I              图片信息             │
│   ,              设置                 │
│   ?              本面板               │
│   Esc            返回 / 退出          │
└────────────────────────────────────────┘
```

---

## 12. 错误处理与边界情况

### 12.1 错误场景

| 场景 | 提示信息 | 处理方案 |
|------|----------|----------|
| 不支持的文件格式 | "不支持该文件格式，请上传 JPG/PNG/WebP/GIF" | 上传区域标红，3 秒后恢复 |
| 文件过大 (>50MB) | "文件过大 (XX MB)，建议压缩至 50MB 以下" | 提供"继续上传"和"取消"选项 |
| 图片损坏 | "无法读取该图片，文件可能已损坏" | 上传区域标红 |
| 粘贴板无图片 | "剪贴板中未检测到图片" | Toast 提示 2 秒 |
| 两张图尺寸不同 | "检测到图片尺寸不同，是否自动对齐？" | 弹窗提供对齐选项 |
| 内存不足 | "图片过大导致内存不足，建议缩小图片" | Toast 提示；图片超出浏览器 canvas 上限时触发 |
| WebGL 不支持 | "您的浏览器不支持部分高级功能" | 降级为纯 Canvas 渲染 |

### 12.2 加载状态

| 状态 | UI 表现 |
|------|---------|
| 上传中 | 上传区域显示像素风加载动画: `█▀█ █▀█ █▀█` 依次高亮 |
| 计算差异中 | 画布区域显示进度条: `正在计算差异... 45%` |
| 首次渲染 | 画布从模糊到清晰的过渡动画 (200ms) |

### 12.3 大图处理

对于超大图片 (>2000px 边长):
1. 首次加载时使用降采样渲染 (0.5x 或 0.25x)
2. 用户缩放到 100% 以上时切换为全分辨率
3. 使用 WebWorker 在后台计算差异，不阻塞主线程
4. 显示加载进度条

---

## 13. 性能优化策略

### 13.1 渲染优化

| 策略 | 说明 |
|------|------|
| **离屏 Canvas** | 差异计算在离屏 Canvas 进行，不阻塞主 Canvas |
| **WebWorker** | 大图差异计算放在 WebWorker 中 |
| **分级渲染** | 缩放 < 50% 时使用降采样渲染 |
| **脏区检测** | 平移/缩放时只重绘可见区域 |
| **请求动画帧** | 所有动画使用 rAF，避免丢帧 |
| **Canvas 缓存** | 已渲染的 Canvas 缓存为 Bitmap，避免重复绘制 |

### 13.2 内存优化

| 策略 | 说明 |
|------|------|
| **复用 ImageData** | 差异计算复用已分配的 ImageData buffer |
| **及时释放** | 组件卸载时释放所有 Canvas 引用 |
| **懒加载** | 变更列表的缩略图按需生成 |
| **限制历史** | 撤销/重做栈最多保留 50 步 |

### 13.3 性能基准

| 操作 | 目标性能 |
|------|----------|
| 1920×1080 差异计算 | < 500ms |
| 4096×4096 差异计算 | < 3s (WebWorker) |
| 缩放动画 | 60fps |
| 平移帧率 | 60fps |
| 首次渲染 | < 1s |

---

## 14. 技术架构

### 14.1 技术栈

| 层 | 技术 |
|----|------|
| 框架 | React 18 + Vite |
| 样式 | CSS (无 UI 库) |
| 图像处理 | Canvas API + WebWorker |
| 状态管理 | React useState/useReducer |
| 构建 | Vite 8 |

### 14.2 目录结构

```
pixeltrace/
├── public/
├── src/
│   ├── components/
│   │   ├── ImageUploader.jsx      # 图片上传组件
│   │   ├── ComparisonView.jsx     # 比较视图容器
│   │   ├── SliderView.jsx         # 滑块视图
│   │   ├── SplitView.jsx          # 分割视图
│   │   ├── FadeView.jsx           # 淡化视图
│   │   ├── BlinkView.jsx          # 闪烁视图
│   │   ├── SubtractView.jsx       # 相减视图
│   │   ├── HighlightView.jsx      # 高亮视图
│   │   ├── ChangeList.jsx         # 变更列表侧边栏
│   │   ├── Toolbar.jsx            # 主工具栏
│   │   ├── ZoomBar.jsx            # 缩放工具栏
│   │   ├── StatusBar.jsx          # 底部状态栏
│   │   ├── SettingsModal.jsx      # 设置面板
│   │   ├── ShortcutHelp.jsx       # 快捷键帮助
│   │   └── ImageInfoPanel.jsx     # 图片信息面板
│   ├── hooks/
│   │   ├── useZoomPan.js          # 缩放/平移 Hook
│   │   ├── useImageDiff.js        # 差异计算 Hook
│   │   ├── useKeyboard.js         # 快捷键 Hook
│   │   └── useExport.js           # 导出 Hook
│   ├── workers/
│   │   └── diffWorker.js          # WebWorker 差异计算
│   ├── utils/
│   │   ├── imageDiff.js           # 核心差异算法
│   │   ├── imageUtils.js          # 图片工具函数
│   │   └── constants.js           # 常量定义
│   ├── App.jsx
│   ├── App.css
│   ├── index.css
│   └── main.jsx
├── index.html
├── package.json
└── vite.config.js
```

### 14.3 数据流

```
用户操作
   │
   ▼
React 组件 (UI 层)
   │
   ├─→ useZoomPan (缩放/平移状态)
   │      │
   │      ▼
   │   Canvas 渲染
   │
   ├─→ useImageDiff (差异计算)
   │      │
   │      ▼
   │   WebWorker → Canvas 渲染
   │
   └─→ useExport (导出)
          │
          ▼
       Canvas → Blob → 下载/剪贴板
```

---

## 15. 视觉设计规范

### 15.1 配色方案

```css
:root {
  /* 背景 */
  --bg-dark: #0a0a0f;           /* 主背景 */
  --bg-card: #12121a;           /* 卡片背景 */
  --bg-hover: #1a1a28;          /* 悬浮背景 */

  /* 强调色 */
  --accent: #00ff88;            /* 主强调色 (像素绿) */
  --accent-dim: #00cc6a;        /* 深一点的绿 */
  --danger: #ff3366;            /* 危险/差异色 */
  --danger-dim: #cc2952;        /* 深一点的红 */

  /* 文字 */
  --text: #e0e0e8;              /* 主文字 */
  --text-dim: #6a6a7a;          /* 次要文字 */

  /* 边框 */
  --border: #2a2a3a;            /* 边框色 */

  /* 特效 */
  --glow-green: 0 0 20px rgba(0, 255, 136, 0.15);
  --glow-red: 0 0 20px rgba(255, 51, 102, 0.15);
}
```

### 15.2 字体

```css
/* 像素风标题/标签 */
font-family: 'Press Start 2P', cursive;

/* 等宽正文/UI */
font-family: 'JetBrains Mono', monospace;
```

### 15.3 视觉特效

| 特效 | 实现方式 |
|------|----------|
| 扫描线背景 | `repeating-linear-gradient` 叠加 |
| 霓虹发光 | `box-shadow` + `text-shadow` |
| 脉冲动画 | CSS `@keyframes pulse` |
| 边框虚线 | `border: 2px dashed` |
| 渐入渐出 | CSS `transition: opacity 200ms` |

### 15.4 响应式断点

| 断点 | 布局调整 |
|------|----------|
| > 1200px | 完整布局，侧边栏展开 |
| 768px ~ 1200px | 侧边栏可折叠 |
| < 768px | 上传区纵向排列，视图按钮堆叠，侧边栏隐藏 |

---

## 16. 无障碍设计

### 16.1 键盘导航

- 所有交互元素可通过 Tab 键聚焦
- 焦点环: 2px 绿色虚线
- 焦点顺序: 从上到下、从左到右

### 16.2 屏幕阅读器

- 所有按钮有 `aria-label`
- 图片区域有 `aria-description`
- 状态变化有 `aria-live` 通告

### 16.3 对比度

- 文字与背景对比度 ≥ 4.5:1 (WCAG AA)
- 强调色与背景对比度 ≥ 7:1 (WCAG AAA)

---

## 17. 未来迭代路线图

### v1.1 (近期)

- [ ] 图片缩放/平移完整实现
- [ ] 完整键盘快捷键系统
- [ ] 侧边栏变更列表完善
- [ ] 错误处理和加载状态
- [ ] 响应式移动端适配

### v1.2 (中期)

- [ ] 图片对齐功能 (自动 + 手动)
- [ ] 撤销/重做系统
- [ ] 图片信息 EXIF 读取
- [ ] 多种导出格式
- [ ] 图片旋转/翻转

### v2.0 (远期)

- [ ] 历史记录 (localStorage)
- [ ] URL 参数化分享
- [ ] 多图批量对比
- [ ] PDF 页面对比
- [ ] AI 差异摘要 (用自然语言描述差异)
- [ ] 插件系统 (自定义差异算法)
- [ ] PWA 离线支持

---

## 附录 A: 当前代码问题清单

> 更新于 2026-09-11。原清单（「无缩放/平移」「无键盘快捷键」「无设置面板」等）
> 已全部修完，下面替换为当前实际遗留项。

| 问题 | 位置 | 优先级 |
|------|------|--------|
| 「↻ 旋转 90°」未实现（§9.2） | 全局绘制路径 | 🟡 中 |
| 「渲染精度 1x/0.5/0.25」未实现（§9.3） | 六个视图 + 差异计算 | 🟡 中 |
| 导出选项面板（格式/质量/水印）未实现（§10.2） | Toolbar | 🟡 中 |
| PNG 全分辨率 / 差异报告 / 变更列表 CSV 未实现（§10.1） | Toolbar | 🟡 中 |
| 变更区域右键菜单未实现（§8.3） | ChangeList | 🟢 低 |
| 双击列表项进入局部聚焦模式未实现（§8.3） | ComparisonView | 🟢 低 |
| `react-hooks` 的 5 条 warning 属既有架构模式，未消 | 多处 | 🟢 低 |
| `INEFFECTIVE_DYNAMIC_IMPORT`：imageDiff.js 既被动态又被静态导入 | useImageDiff / ComparisonView | 🟢 低 |
